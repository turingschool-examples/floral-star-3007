<p><%= @mechanic.name %></p>
<p><%= @mechanic.years_experience %></p>
<p>Rides Working On:</p>
<% @rides.each do |ride| %>
    <p><%= ride.name %></p>
<% end %>

<%= form_with url: "/mechanics/#{@mechanic.id}", action: :add_ride, local: true do |f| %>
  <%= f.label :ride_id, "Assign Ride" %>
  <%= f.text_field :ride_id, :size => "10" %>
  <%= f.submit "Assign Ride" %>
<% end %>

